# Smithery Configuration File
# https://smithery.ai/docs/build/publish

startCommand:
  type: sse
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: "Optional Civify Developer API key (starts with 'cv-fy-'). You can also sign in interactively in chat using civify_login."
  url: https://mcp.civify.cv/sse
